#include <stdio.h>
// Funcion para numero mayor
    int num_mas_grande(x, y, z)
    {
        imprimir = printf("Imprimo numero %d", z);

        return z;
    }


// Funcion mas grande
    int main ()
    {
        int mayor = 0;
        int x = 5;
        int y = 10;
        int z = 15;
        return 0;
        num_mas_grande(imprimir);
        mayor = num_mas_grande(x, y, z);
    }